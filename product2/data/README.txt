the data in the UBOS_data.csv is just the matching filter name and the real values extracted from the UBOS attribute table.

FilterList, has the matching attribute column name attached to the value we want to show on the portal and which theme it belongs to....so....

"HH_own_Radio_2014"= "Owns a Radio" which is of the theme (Socio-Economic (% of households))


The geoJSON, the shape has matching names to the units described in the UBOS_data sheet.
